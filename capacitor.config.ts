import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'HITO2-T2-Pokedex',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
